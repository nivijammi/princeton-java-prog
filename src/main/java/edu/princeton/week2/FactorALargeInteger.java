package edu.princeton.week2;
/*
1.3.28 Run quick experiments to determine the impact of using the termination condition (factor <= n/factor) instead of (factor < n) in Factors in Program 1.3.9.
For each method, find the largest n such that when you type in an n-digit number, the program is sure to finish within 10 seconds.
 */
public class FactorALargeInteger {
    public long findTheFactor(long n, long factor){

        return n;
    }
    public static void main(String[] args) {

    }
}
